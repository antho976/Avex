<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Match the Compose-side ForgeBackground so there's no flash on cold launch. -->
    <color name="forge_background">#0B0B0E</color>
    <!-- Matches the play-store icon asset's canvas so the launcher icon reads identically. -->
    <color name="ic_launcher_background">#141414</color>
</resources>
