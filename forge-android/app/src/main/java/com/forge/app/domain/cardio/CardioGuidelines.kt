package com.forge.app.domain.cardio

/**
 * Public physical-activity reference points (GYMAP-42). The World Health Organization recommends at
 * least 150 minutes of moderate-intensity aerobic activity per week for adults — the cardio screen
 * fills a reference meter toward this when the user hasn't set their own weekly minutes target, so a
 * fresh user still has a baseline to read against instead of a blank space.
 */
const val WHO_WEEKLY_ACTIVITY_MIN = 150
