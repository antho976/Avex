# Forge — Bug Fix Plan

_2026-06-07. 80 confirmed bugs from the deep audit ([CODE_AUDIT.md](CODE_AUDIT.md)), grouped into 14 sections to clear one at a time. Order ≈ priority (data-safety & crashes first). Check items off as we go._

**Legend:** 🔴 critical · 🟠 high · 🟡 medium · ⚪ low · (×2 = independently flagged by two reviewers)

| # | Section | Bugs |
|---|---|---|
| 1 | Crashes & hard failures | 5 |
| 2 | Home-screen widget rewrite | 3 |
| 3 | Backup / Restore / Reset / Export data-safety | 9 |
| 4 | Weight units (kg ⇄ lb) — centralize conversion | 7 |
| 5 | Custom & added exercises (effective-plan resolution) | 8 |
| 6 | Swap & customization field preservation | 2 |
| 7 | Bodyweight one-per-day (DB migration) | 3 |
| 8 | Warmup gate sizing | 1 |
| 9 | Broken feature wiring | 2 |
| 10 | Stats, insights & trophy math | 15 |
| 11 | Recap & weekly summary | 4 |
| 12 | Program generation & sample data | 6 |
| 13 | Cardio & settings | 4 |
| 14 | Session flow & misc UI-state | 11 |

---

## 1. Crashes & hard failures (5)

> Quick, isolated wins that stop hard failures. The Android-14 foreground-service one needs a real-device check.

- [x] **🔴 CRIT** SampleDataSeeder crashes onboarding for any non-4-day generated program (NoSuchElementException)
  - [data/repo/SampleDataSeeder.kt:37,44,52](forge-android/app/src/main/java/com/forge/app/data/repo/SampleDataSeeder.kt#L37)
  - Fix: Drive the seeder off the active program instead of hard-coded keys: iterate `Program.days` (e.g. cycle `Program.dayKeys`) rather than the fixed dayRotation list, or guard each lookup with…
- [x] **🟠 HIGH** Foreground service started with FOREGROUND_SERVICE_TYPE_HEALTH but lacks the required runtime permission — SecurityException on Android 14+ (targetSdk 35) — _switched to specialUse; ⚠️ verify on a real Android 14+ device_
  - [service/WorkoutSessionService.kt:80-84](forge-android/app/src/main/java/com/forge/app/service/WorkoutSessionService.kt#L80)
  - Fix: Switch the FGS type to one whose prerequisites the app actually satisfies — e.g. FOREGROUND_SERVICE_TYPE_SPECIAL_USE (manifest foregroundServiceType="specialUse" + a <property> subtype and the…
- [x] **🟠 HIGH** Duplicate LazyColumn key crashes PRs list when one exercise PRs twice in the same session
  - [ui/gym/stats/PrsContent.kt:159](forge-android/app/src/main/java/com/forge/app/ui/gym/stats/PrsContent.kt#L159)
  - Fix: Carry RecentPrRow.loggedExerciseId through PrDisplayRow and use it in the key: key = { it.loggedExerciseId } (or "${it.exerciseId}_${it.loggedExerciseId}"). loggedExerciseId is the autogenerated…
- [x] **⚪ LOW** stringToEffortRating throws NoSuchElementException on any unrecognized difficulty code (Room read crash)
  - [data/db/types/EffortRating.kt:19-20](forge-android/app/src/main/java/com/forge/app/data/db/types/EffortRating.kt#L19)
  - Fix: Make the converter tolerant: `fun fromCodeOrNull(code: String): EffortRating? = entries.firstOrNull { it.code == code }` and have stringToEffortRating return null for unknown codes (treat as 'no…
- [x] **⚪ LOW** MuscleGroup/ExerciseUnit/Difficulty.fromCode throw NoSuchElementException on any unknown code
  - [program/Types.kt:22-23, 39-40, 50-51](forge-android/app/src/main/java/com/forge/app/program/Types.kt#L22)
  - Fix: Make all three fromCode functions return a nullable (firstOrNull) like ProblemArea.fromCode, or provide a safe default, so a bad persisted code degrades gracefully instead of throwing.

## 2. Home-screen widget rewrite (3)

> One rewrite — share the Hilt singleton DB via an EntryPoint — fixes the critical data-wipe, the connection leak, AND the wrong-day rotation at once.

- [x] **🔴 CRIT** Widget opens the live DB with destructive-migration fallback — silent total data wipe on any future schema bump
  - [widget/ForgeWidget.kt:33-35](forge-android/app/src/main/java/com/forge/app/widget/ForgeWidget.kt#L33)
  - Fix: Do not let the widget open the DB with a divergent config. Either obtain the Hilt-provided singleton ForgeDatabase (via an EntryPoint) so the widget shares the exact same migration-aware instance, or…
- [x] **🟠 HIGH** Widget leaks a Room/SQLite connection on every (hourly) update — never closes the DB
  - [widget/ForgeWidget.kt:33-41](forge-android/app/src/main/java/com/forge/app/widget/ForgeWidget.kt#L33)
  - Fix: Wrap usage in db.use { ... } / call db.close() in a finally, or better, reuse the singleton ForgeDatabase via a Hilt EntryPoint so no new instance is created per update.
- [x] **🟡 MED** Widget 'next day' uses a hardcoded 4-day upper/lower rotation and may read an un-loaded Program — wrong day/exercises for custom programs — _verify the widget still renders on-device_
  - [widget/ForgeWidget.kt:36-41](forge-android/app/src/main/java/com/forge/app/widget/ForgeWidget.kt#L36)
  - Fix: Derive the next day from the DB-backed program (ProgramDao.days() ordered by position) rather than a hardcoded key rotation, and ensure the program is loaded (await ensureLoaded() / read program_day…

## 3. Backup / Restore / Reset / Export data-safety (9)

> Harden the destructive + restore paths. Consider routing factory reset through clearAllTables() so future tables can never be missed.

- [x] **🟠 HIGH** Factory reset leaves most feature data behind (bodyweight, mood, goals, vacations, program edits, etc. survive) (×2)
  - [data/repo/ResetRepository.kt:23-28](forge-android/app/src/main/java/com/forge/app/data/repo/ResetRepository.kt#L23)
  - Fix: Inject the remaining feature DAOs into ResetRepository and call their deleteAll/clear in factoryReset() (and add a MoodDao.deleteAll() / per-table clear where missing). Better: implement factory…
- [x] **🟠 HIGH** Restore does not validate schema version — restoring a newer-version backup crashes the app on next launch after the live DB is already overwritten
  - [data/repo/BackupRepository.kt:243-271](forge-android/app/src/main/java/com/forge/app/data/repo/BackupRepository.kt#L243)
  - Fix: Before swapping, open the candidate read-only and read PRAGMA user_version; reject (return false) if it is greater than the app's current schema version (or otherwise out of the supported migration…
- [x] **🟡 MED** resetAppSettings()/resetAll() clears ALL preferences including onboarding-done and welcomed flags — 'reset settings' silently re-triggers onboarding
  - [data/prefs/SettingsRepository.kt:330-333](forge-android/app/src/main/java/com/forge/app/data/prefs/SettingsRepository.kt#L330)
  - Fix: Reset only the user-facing preference keys (remove them individually or restore documented defaults) and explicitly preserve ONBOARDING_DONE, WELCOMED, USER_NAME, and USER_GOAL — or have factoryReset…
- [x] **⚪ LOW** snapshotDatabase / VACUUM INTO can fail silently leaving stale temp files; failure inside snapshot leaks the temp .db
  - [data/repo/BackupRepository.kt:215-236](forge-android/app/src/main/java/com/forge/app/data/repo/BackupRepository.kt#L215)
  - Fix: Move the snapshotDatabase() call inside the try block (or wrap it so the temp file is deleted on any failure), and consider deleting the destination immediately before VACUUM INTO. Use a…
- [x] **⚪ LOW** exportFullBackup omits all DataStore preferences despite documenting 'settings' — backup is not a complete restore source
  - [data/repo/BackupRepository.kt:105-179](forge-android/app/src/main/java/com/forge/app/data/repo/BackupRepository.kt#L105)
  - Fix: Either serialize the relevant SettingsRepository values into the JSON (and write a matching importer) or, more simply, also back up the DataStore preferences file alongside the DB in…
- [x] **⚪ LOW** exportWeeklyJson uses started_at window so sessions started before the week boundary but finished within it are dropped from the weekly AI export
  - [data/repo/BackupRepository.kt:42-45](forge-android/app/src/main/java/com/forge/app/data/repo/BackupRepository.kt#L42)
  - Fix: Decide the intended semantics: if it is 'sessions finished in the last 7 days', filter by finished_at, not started_at; if it is 'the current calendar week', compute the week start from the user's…
- [x] **⚪ LOW** PDF export silently truncates long sessions (exercises and overflow sets beyond the single A4 page are dropped with no indication) — _adds an "… N more not shown" note; multi-page PDF still a future enhancement_
  - [data/repo/PdfExportRepository.kt:77-88](forge-android/app/src/main/java/com/forge/app/data/repo/PdfExportRepository.kt#L77)
  - Fix: Paginate: when y exceeds the printable area, finishPage and startPage a new page, resetting y; guard the inner set loop the same way as the exercise loop; or at minimum draw a '… N more exercises not…
- [x] **⚪ LOW** exportLastSessionPdf relies on allFinished() list order to pick the 'last' session — fragile coupling to ORDER BY started_at ASC
  - [data/repo/PdfExportRepository.kt:35-38](forge-android/app/src/main/java/com/forge/app/data/repo/PdfExportRepository.kt#L35)
  - Fix: Add a dedicated DAO query (e.g. SELECT * FROM session WHERE finished_at IS NOT NULL ORDER BY finished_at DESC LIMIT 1) and use it here instead of allFinished().lastOrNull().
- [x] **⚪ LOW** restoreFromUri closes the Room singleton but other active Flows/coroutines can touch the now-closed DB before the process is killed
  - [data/repo/BackupRepository.kt:252-259](forge-android/app/src/main/java/com/forge/app/data/repo/BackupRepository.kt#L252)
  - Fix: Kill/restart the process from within the restore path immediately after the swap (or signal collectors to stop first), rather than closing Room and returning to the UI and hoping no DB access occurs…

## 4. Weight units (kg ⇄ lb) — centralize conversion (7)

> Root cause: conversion is applied at log but not at edit/autofill/delta. Funnel all weight entry/display through one helper pair, then these collapse.

- [x] **🟠 HIGH** parseToLb cannot round-trip formatWeight output: tapping a kg suggestion logs a set with no weight
  - [domain/units/WeightFormatter.kt:15-18](forge-android/app/src/main/java/com/forge/app/domain/units/WeightFormatter.kt#L15)
  - Fix: Make parseToLb tolerant of the unit suffix it pairs with formatWeight, e.g. val numeric = input.trim().removeSuffix("kg").removeSuffix("lb").trim().toDoubleOrNull() ?: return null. Better: have the…
- [x] **🟠 HIGH** Editing a set in kg mode silently corrupts the stored weight (no kg→lb conversion on the edit path)
  - [ui/gym/train/components/SetRow.kt:77-80, 138](forge-android/app/src/main/java/com/forge/app/ui/gym/train/components/SetRow.kt#L77)
  - Fix: Seed editWeight from the display unit (the converted number without the unit suffix) and convert the entered value with parseToLb(useKg) before emitting, OR pass useKg through to the EditSet handler…
- [x] **🟡 MED** "try X × Y" autofill injects a unit-suffixed string that fails to parse in kg mode
  - [ui/gym/train/components/SetInputRow.kt:201-214](forge-android/app/src/main/java/com/forge/app/ui/gym/train/components/SetInputRow.kt#L201)
  - Fix: Autofill with a bare numeric display value, not the suffixed formatWeight string — format the converted display number without the unit (strip the ' kg'/' lb'), mirroring what the user would type.…
- [x] **🟡 MED** Set delta is computed and labelled in pounds even when the app is in kg mode
  - [ui/gym/train/components/SetRow.kt:96-106](forge-android/app/src/main/java/com/forge/app/ui/gym/train/components/SetRow.kt#L96)
  - Fix: Convert wDiff to the display unit and use unitLabel(useKg) for the suffix, consistent with formatWeight used elsewhere in the row.
- [x] **🟡 MED** Editing a logged set in kg mode is unitless and silently stores the kg number as pounds
  - [ui/gym/train/DaySessionContent.kt:177](forge-android/app/src/main/java/com/forge/app/ui/gym/train/DaySessionContent.kt#L177)
  - Fix: Make the edit path symmetric with the log path: seed the edit field with the kg-formatted value when useKg, label it with the active unit, and convert kg→lb (parseToLb) before dispatching EditSet —…
- [x] **⚪ LOW** Set delta weight difference is truncated to Int, dropping fractional/small increments
  - [ui/gym/train/components/SetRow.kt:100-103](forge-android/app/src/main/java/com/forge/app/ui/gym/train/components/SetRow.kt#L100)
  - Fix: Format the delta with rounding rather than truncation and preserve one decimal where needed (e.g. reuse formatWeight on abs(wDiff) with an explicit sign, or use Math.round / %+.1f). Display in the…
- [x] **⚪ LOW** Per-set delta uses prior set by list index and treats bodyweight (null weightLb) prior as 0 lb — _bodyweight-as-0 fixed (falls back to rep delta); index-based prior is by design_
  - [ui/gym/train/components/SetRow.kt:96-99](forge-android/app/src/main/java/com/forge/app/ui/gym/train/components/SetRow.kt#L96)
  - Fix: Only compute a weight delta when both current and prior weightLb are non-null; fall back to the reps delta (or no delta) when either side is unweighted.

## 5. Custom & added exercises (effective-plan resolution) (8)

> Root cause: handlers resolve against the static dayPlan while the UI renders effectivePlanForDay. Centralize plan/order resolution on the effective/rendered list.

- [x] **🟠 HIGH** Editing reps/sets on a program exercise silently moves it to the bottom of the day (orderOverride default 999 defeats natural-order fallback) (×2)
  - [data/repo/ProgramCustomizationRepository.kt:58-64](forge-android/app/src/main/java/com/forge/app/data/repo/ProgramCustomizationRepository.kt#L58)
  - Fix: Treat 999 as 'no explicit order' in the sort, e.g. `customByExId[plan.id]?.orderOverride?.takeIf { it != 999 } ?: (i * 10)`; or change orderOverride to a nullable Int? defaulting to null so the…
- [x] **🟠 HIGH** Unplanned exercises added mid-session are written to DB but vanish from the UI after refresh
  - [ui/gym/train/DayExerciseHandlers.kt:134-144](forge-android/app/src/main/java/com/forge/app/ui/gym/train/DayExerciseHandlers.kt#L134)
  - Fix: refreshExercises() must also render logged exercises in the session that are not part of effectivePlanForDay (build an ExercisePlan from Program.exercise(id)/ExerciseLibrary for them and append), OR…
- [x] **🟠 HIGH** logSet / EditSet silently no-op for custom or cross-day exercises (gated on dayPlan.exercises)
  - [ui/gym/train/DayExerciseHandlers.kt:201](forge-android/app/src/main/java/com/forge/app/ui/gym/train/DayExerciseHandlers.kt#L201)
  - Fix: Resolve the plan from the rendered state (_state.value.exercises.firstOrNull { it.plan.id == exerciseId }?.plan) or from effectivePlanForDay/Program.exercise(id), not from dayPlan.exercises. Compute…
- [x] **🟠 HIGH** Add-exercise from the picker silently does nothing unless the exercise is already in the current day's plan
  - [ui/gym/train/DayScreen.kt:208](forge-android/app/src/main/java/com/forge/app/ui/gym/train/DayScreen.kt#L208)
  - Fix: Either (a) merge session-added logged_exercise rows (resolved via Program.exercise(id)) into the list built by refreshExercises so they render, or (b) restrict AddExerciseSheet to exercises that…
- [x] **🟡 MED** Manual exercise reorder (MoveExercise) is discarded by any refreshExercises() — _now preserved within the session; cross-navigation persistence would need an orderOverride write_
  - [ui/gym/train/DayExerciseHandlers.kt:121-129](forge-android/app/src/main/java/com/forge/app/ui/gym/train/DayExerciseHandlers.kt#L121)
  - Fix: Persist the reorder via programCustomRepo orderOverride (or a session-scoped order) so refreshExercises preserves it; or have refreshExercises honor the current in-memory ordering for already-present…
- [x] **🟡 MED** DayEditSheet exercise list never shows custom-added exercises or honours order overrides
  - [ui/overview/DayEditSheet.kt:181-197](forge-android/app/src/main/java/com/forge/app/ui/overview/DayEditSheet.kt#L181)
  - Fix: Drive the exercise rows from repo.effectivePlanForDay(selectedDayKey) (or expose an effective-plan flow from DayEditViewModel) instead of selectedDay.exercises, so added exercises and orderOverride…
- [x] **⚪ LOW** ProgramEditorScreen mutates ViewModel state from inside remember{} during composition
  - [ui/programeditor/ProgramEditorScreen.kt:138](forge-android/app/src/main/java/com/forge/app/ui/programeditor/ProgramEditorScreen.kt#L138)
  - Fix: Use LaunchedEffect(dayKey) { viewModel.setDay(dayKey) } instead of mutating inside remember.
- [x] **⚪ LOW** ProgramEditorItem.removed is hard-coded false and effectivePlanForDay already drops removed rows, so there is no way to restore a removed exercise
  - [ui/programeditor/ProgramEditorScreen.kt:86-100, 117-119](forge-android/app/src/main/java/com/forge/app/ui/programeditor/ProgramEditorScreen.kt#L86)
  - Fix: Either surface removed exercises in the editor (include them from observeForDay with a restore action) or remove the dead `removed` field and document Reset as the recovery path.

## 6. Swap & customization field preservation (2)

> Both rebuild an entity positionally and drop fields. Switch to .copy() so untouched columns survive.

- [x] **🟠 HIGH** setSwap silently wipes the user's pinned note (rebuilds ExerciseCustomization without preserving pinnedNote)
  - [data/repo/CustomizationRepository.kt:27-33](forge-android/app/src/main/java/com/forge/app/data/repo/CustomizationRepository.kt#L27)
  - Fix: Use copy() to preserve untouched fields, mirroring the other setters: `val existing = customizationDao.get(exerciseId); customizationDao.upsert(existing?.copy(swappedName = swappedName, swappedUnit =…
- [x] **🟡 MED** Session swap wipes the exercise's superset group (and resets hitFullTarget)
  - [ui/gym/train/DaySwapHandlers.kt:24-38](forge-android/app/src/main/java/com/forge/app/ui/gym/train/DaySwapHandlers.kt#L24)
  - Fix: Load the existing LoggedExercise (loggedExerciseDao.get(leId)) and `.copy(swappedName = ..., swappedUnit = ...)` it, OR add a targeted DAO update for swappedName/swappedUnit only, so…

## 7. Bodyweight one-per-day (DB migration) (3)

> Needs a real MIGRATION_14_15 (schema is locked from v12): de-dupe existing rows, add a UNIQUE index on date_key, export schema JSON, add a MigrationTest.

- [x] **🟠 HIGH** BodyweightEntry 'one entry per day' upsert is unenforced — duplicate rows per date — _MIGRATION_14_15; ⚠️ run connectedAndroidTest / verify on device_
  - [data/db/entities/BodyweightEntry.kt:8-16](forge-android/app/src/main/java/com/forge/app/data/db/entities/BodyweightEntry.kt#L8)
  - Fix: Add a unique index on `date_key` so REPLACE actually upserts: `@Entity(tableName = "bodyweight_entry", indices = [Index(value = ["date_key"], unique = true)])`. This requires a real migration (schema…
- [x] **🟡 MED** Bodyweight "upsert by date" creates duplicate rows per day (REPLACE keys on id, not date_key)
  - [data/db/dao/BodyweightDao.kt:13-14](forge-android/app/src/main/java/com/forge/app/data/db/dao/BodyweightDao.kt#L13)
  - Fix: Make date_key the natural conflict key: either set @PrimaryKey on dateKey (dropping autoGenerate id) or add @Entity(indices=[Index(value=["date_key"], unique=true)]) plus a migration, so REPLACE…
- [x] **⚪ LOW** BodyweightRepository.log() creates duplicate same-day rows instead of upserting by date
  - [data/repo/BodyweightRepository.kt:20](forge-android/app/src/main/java/com/forge/app/data/repo/BodyweightRepository.kt#L20)
  - Fix: Add a unique index on date_key (@Entity(indices=[Index(value=["date_key"], unique=true)])) via a real migration so REPLACE dedupes by day, OR change log() to look up the existing day's row id and…

## 8. Warmup gate sizing (1)

> Single fix: size warmupChecks to the actual warmup list length instead of List(4).

- [x] **🟠 HIGH** Warmup gate is uncompletable (or auto-completes early) when custom warmup count != 4 (×2)
  - [ui/gym/train/state/DayUiState.kt:37](forge-android/app/src/main/java/com/forge/app/ui/gym/train/state/DayUiState.kt#L37)
  - Fix: Size warmupChecks to the actual warmup list length. Derive the list length from customWarmupItems ?: dayPlan.warmup when initializing/resizing warmupChecks, and base isWarmupComplete on `updated.size…

## 9. Broken feature wiring (2)

> Two high-visibility features that silently do nothing / lag.

- [x] **🟠 HIGH** "Train X today" / planned-next-day is a dead write — never affects Today or Start session
  - [ui/overview/OverviewScreen.kt:104, 113-116, 188-224](forge-android/app/src/main/java/com/forge/app/ui/overview/OverviewScreen.kt#L104)
  - Fix: Either consume plannedNextDay when deriving the effective "next up" day (e.g. nextUpDayKey = plannedNextDay.takeIf { it.isNotBlank() } ?: stats.nextUpDayKey, clearing the pref when a session for that…
- [x] **🟠 HIGH** Notes search field lags / drops characters — TextField value driven by a debounced async flow
  - [ui/gym/notes/NotesSearchViewModel.kt:32-42](forge-android/app/src/main/java/com/forge/app/ui/gym/notes/NotesSearchViewModel.kt#L32)
  - Fix: Drive the TextField from an immediate snapshot/StateFlow that `setQuery` updates synchronously (e.g. expose `query` from `_query` directly, or hold a separate `mutableStateOf`/StateFlow for the raw…

## 10. Stats, insights & trophy math (15)

> Aggregation/keying/threshold correctness. Mostly pure functions — pair each fix with a unit test.

- [x] **🟡 MED** Deload insight compares set-timestamps, not sessions (missing .distinct()), so the 'recent 6 sessions' window collapses
  - [data/repo/StatsEffortAggregations.kt:190-200](forge-android/app/src/main/java/com/forge/app/data/repo/StatsEffortAggregations.kt#L190)
  - Fix: Compute per-session totals first: group `allSets` by `sessionStartedAt`, take the 6 most recent DISTINCT sessions, split those 6 into two groups of 3, and compare summed volume. e.g. `val sessions =…
- [x] **🟡 MED** TrophyRepository near-miss recording uses the wrong set (passes unlocked-not-satisfied instead of locked trophies)
  - [data/repo/TrophyRepository.kt:85,93](forge-android/app/src/main/java/com/forge/app/data/repo/TrophyRepository.kt#L85)
  - Fix: Pass the locked-and-not-satisfied set: `recordNearMisses(snapshot, Trophies.all.mapTo(mutableSetOf()){it.id} - already - satisfied)` (or at least `- already`).
- [x] **🟡 MED** PrDetector ignores isAssisted, contradicting the documented 'excluded from all-time PR comparison' contract
  - [domain/pr/PrDetector.kt:25-32](forge-android/app/src/main/java/com/forge/app/domain/pr/PrDetector.kt#L25)
  - Fix: Either drop the entity contract, or honor it: filter history with `!it.isAssisted` inside isPr and have the caller skip assisted current sets (pass the flag in, or exclude in computePrFlags).
- [x] **🟡 MED** Strength radar uses library ids but its data map is keyed by old slot ids — card never renders correct data
  - [ui/gym/stats/components/AdvancedChartsExtra.kt:32-42](forge-android/app/src/main/java/com/forge/app/ui/gym/stats/components/AdvancedChartsExtra.kt#L32)
  - Fix: Make the two id lists identical. Change RADAR_EXERCISE_IDS in StatsStrengthAggregations.kt to the library ids used by RADAR_IDS (the ids logged sets now store), or share a single constant between the…
- [x] **🟡 MED** StrengthOverlayCard legend is a no-op — comparison chart's two curves are unlabeled
  - [ui/gym/stats/components/AdvancedCharts.kt:81](forge-android/app/src/main/java/com/forge/app/ui/gym/stats/components/AdvancedCharts.kt#L81)
  - Fix: Implement LegendItem as a @Composable: a small colored dot/swatch plus the label Text, e.g. a Row with a Box(Modifier.size(8.dp).background(color, CircleShape)) and Text(label). Mark it @Composable…
- [x] **🟡 MED** vs-AVG % and BEST badge compute average/max including the session being compared
  - [ui/overview/OverviewUiStateMapper.kt:46-51](forge-android/app/src/main/java/com/forge/app/ui/overview/OverviewUiStateMapper.kt#L46)
  - Fix: Compute the comparison baseline excluding the current session (e.g. avg over sessions other than s.id, or query a per-session excludeSessionId aggregate as maxVolumeForDay already does), or document…
- [x] **⚪ LOW** perDayTypeStats avg duration uses integer division, truncating each session before averaging
  - [data/db/dao/SessionDao.kt:189-197](forge-android/app/src/main/java/com/forge/app/data/db/dao/SessionDao.kt#L189)
  - Fix: Use floating-point division so truncation happens once, after averaging: AVG((finished_at - started_at) / 60000.0). The result column is already mapped to Double? (DayTypeStats.avgDurationMin), so no…
- [x] **⚪ LOW** weekDaysTrained filters on finishedAt week-boundary but indexes the day by startedAt — cross-midnight sessions mark the wrong day/week
  - [data/repo/StatsRepository.kt:110-116](forge-android/app/src/main/java/com/forge/app/data/repo/StatsRepository.kt#L110)
  - Fix: Pick one timestamp for both the membership test and the day index. e.g. compute `val d = Instant.ofEpochMilli(it.startedAt).atZone(zone).toLocalDate()` once, then keep it only if…
- [x] **⚪ LOW** findOnThisDayMemory uses a forward-only [day, day+3] window, not the documented ±3-day window
  - [data/repo/StatsRepository.kt:405-416](forge-android/app/src/main/java/com/forge/app/data/repo/StatsRepository.kt#L405)
  - Fix: Center the window on the anniversary: `fromMs = targetMs - windowMs` and `toMs = targetMs + windowMs` (with the ABS center at targetMs or targetMs+12h), to actually search ±3 days.
- [x] **⚪ LOW** buildExerciseYoY counts the current partial year against a full prior year, biasing YoY 'delta' negative early in the year
  - [data/repo/StatsStrengthAggregations.kt:176-196](forge-android/app/src/main/java/com/forge/app/data/repo/StatsStrengthAggregations.kt#L176)
  - Fix: Compare like windows — e.g. trailing 365 days vs the prior 365 days, or gate to only show YoY once the current year has comparable elapsed time / data. At minimum document that thisYearMax is…
- [ ] **⚪ LOW** buildPrEntries matches PR row to sets by (exerciseId + sessionStartedAt), picking the wrong set when an exercise is logged twice in one session — _DEFERRED: needs logged_exercise_id added to the SetWithExerciseAndSession projection + its query; display-only and only wrong in the rare twice-in-one-session case_
  - [data/repo/StatsStrengthAggregations.kt:47-61](forge-android/app/src/main/java/com/forge/app/data/repo/StatsStrengthAggregations.kt#L47)
  - Fix: Carry weight/reps for the PR set directly in the RecentPrRow query (join the specific LoggedExercise's top set), or add loggedExerciseId to the SetWithExerciseAndSession projection and match on it…
- [x] **⚪ LOW** checkComebackKid measures the gap with raw millisecond truncation instead of calendar days
  - [data/repo/TrophyRepository.kt:170,171](forge-android/app/src/main/java/com/forge/app/data/repo/TrophyRepository.kt#L170)
  - Fix: Compute the gap in calendar days: ChronoUnit.DAYS.between(prevDate, currDate) where prevDate/currDate are Instant.ofEpochMilli(...).atZone(zone).toLocalDate(), then test the range.
- [x] **⚪ LOW** Rep Machine trophy unlocks on max reps in a single SET, not summed across the exercise as described
  - [domain/trophy/TrophyStatsSnapshot.kt:29](forge-android/app/src/main/java/com/forge/app/domain/trophy/TrophyStatsSnapshot.kt#L29)
  - Fix: Either rename the field/description to 'reps in a single set', or change the data source to a per-loggedExercise SUM(reps) MAX (GROUP BY logged_exercise_id) so it matches the 'single exercise'…
- [x] **⚪ LOW** Bodyweight delta truncates to 0 for sub-1 lb changes, showing "0"/"+0" as if no change
  - [ui/gym/stats/StatsBodyExtra.kt:46](forge-android/app/src/main/java/com/forge/app/ui/gym/stats/StatsBodyExtra.kt#L46)
  - Fix: Render the delta with one decimal ("%+.1f".format(delta)) or round instead of truncate (delta.roundToInt()), and/or raise the display threshold to >= 1.0 if integer display is desired.
- [ ] **⚪ LOW** Muscle filter silently drops PRs for exercises not in the static program catalogue — _DEFERRED: largely by-design (a custom exercise has no catalogued muscle, so it can't match a specific-muscle filter; it still appears under "All muscles"). Would need to thread the customization muscle into PrsViewModel_
  - [ui/gym/stats/PrsViewModel.kt:72](forge-android/app/src/main/java/com/forge/app/ui/gym/stats/PrsViewModel.kt#L72)
  - Fix: Either persist the muscle on the logged record, or fall back to a stored/last-known muscle, or document that catalogue-less PRs only appear under "All muscles". At minimum, surface them in an "Other"…

## 11. Recap & weekly summary (4)

> Volume math + unit labels + quiet-hours handling in the recap surfaces.

- [x] **🟡 MED** Recap 'Avg weekly volume' divides by sessions/7 instead of calendar weeks — grossly inflated
  - [ui/recap/RecapViewModel.kt:100-101](forge-android/app/src/main/java/com/forge/app/ui/recap/RecapViewModel.kt#L100)
  - Fix: Divide total volume by the number of calendar weeks in the period that has actually elapsed, e.g. weeks = max(1, ChronoUnit.WEEKS.between(yearStartDate, today)+1) or weeks = ceil(daysElapsed / 7.0).…
- [x] **⚪ LOW** Weekly recap notification always shows volume in 'lb', ignoring the user's kg preference
  - [service/WeeklyRecapWorker.kt:41](forge-android/app/src/main/java/com/forge/app/service/WeeklyRecapWorker.kt#L41)
  - Fix: Read settingsRepo.useKg (the worker already injects settingsRepo) and format the volume via the shared unit helpers, e.g. ${(if (useKg) volumeLb*KG_PER_LB else volumeLb).toInt()} ${unitLabel(useKg)}.
- [x] **⚪ LOW** Weekly recap silently dropped (not deferred) when its 7-day trigger lands in quiet hours
  - [service/WeeklyRecapWorker.kt:33](forge-android/app/src/main/java/com/forge/app/service/WeeklyRecapWorker.kt#L33)
  - Fix: When isQuietNow() is true, schedule a one-time delayed WorkRequest to post the recap just after quiet hours end (or return Result.retry() with a backoff that lands outside the window) instead of…
- [x] **⚪ LOW** Recap monthly/yearly volume shown as integer 'k lb' truncates, hiding all volume under 1000 lb as '0k'
  - [ui/recap/RecapScreen.kt:79,96](forge-android/app/src/main/java/com/forge/app/ui/recap/RecapScreen.kt#L79)
  - Fix: Format with rounding and a fractional 'k' below a threshold, e.g. show the raw lb when < 1000, or use String.format("%.1fk", totalVolumeLb/1000) / Math.round so 2900 -> '2.9k' instead of '2k'.

## 12. Program generation & sample data (6)

> Rotation/profile fidelity, undersized presets, and realistic seed data.

- [x] **🟡 MED** Automatic program rotation ignores the user's goal/experience/emphasis/problem-areas/priority-muscles/pinned (builds a near-empty GenerationParams)
  - [data/repo/WorkoutRepository.kt:87-92](forge-android/app/src/main/java/com/forge/app/data/repo/WorkoutRepository.kt#L87)
  - Fix: Have maybeRotateProgram build the same full params as ProgramRepository.currentParams(). Simplest: add a public reroll-with-current-params entry point on ProgramRepository (e.g. `rerollAll()` that…
- [x] **🟡 MED** SampleDataSeeder never sets PRs — seeded sessions have prCount=0 and no was_pr exercises
  - [data/repo/SampleDataSeeder.kt:54,98](forge-android/app/src/main/java/com/forge/app/data/repo/SampleDataSeeder.kt#L54)
  - Fix: Mark a realistic subset of exercises as PRs (e.g. when the working weight exceeds the prior seeded max for that exerciseId), set LoggedExercise.was_pr accordingly, and accumulate prCount before the…
- [x] **🟡 MED** Generator silently produces undersized days for the user-selectable "Dumbbells + bench" preset (BACK x3 vs only 2 available back lifts)
  - [program/ProgramGenerator.kt:84-108](forge-android/app/src/main/java/com/forge/app/program/ProgramGenerator.kt#L84)
  - Fix: When a slot can't be filled with a distinct exercise, fall back to allowing a repeat of an already-used exercise for that muscle (e.g. relax the `usedInDay` exclusion for the last resort) or merge…
- [x] **⚪ LOW** SampleDataSeeder set timestamps can exceed the session's finishedAt and interleave non-monotonically
  - [data/repo/SampleDataSeeder.kt:82,49,50](forge-android/app/src/main/java/com/forge/app/data/repo/SampleDataSeeder.kt#L82)
  - Fix: Accumulate a running set counter across exercises for the time offset (track a mutable index instead of exIdx*plan.sets), and clamp/derive completedAt so the last set is <= finishedMs (e.g. spread…
- [x] **⚪ LOW** restSeconds "heavy" detection uses the MAX rep of the range, so heavy strength slots are never flagged heavy
  - [program/SessionEstimate.kt:22-29](forge-android/app/src/main/java/com/forge/app/program/SessionEstimate.kt#L22)
  - Fix: Base the heavy check on the LOWER bound of the rep range, e.g. `minReps(plan.reps)?.let { if (it <= 8) HEAVY_REST_BONUS else 0 }`, or key it off the slot's RepScheme (STRENGTH) instead of parsing the…
- [x] **⚪ LOW** Emphasis volume bonus can be entirely cancelled by the weekly cap on high-frequency splits, silently undoing the user's emphasis
  - [program/VolumeModel.kt:59-80](forge-android/app/src/main/java/com/forge/app/program/VolumeModel.kt#L59)
  - Fix: Apply emphasis as a cap bump too (raise weeklyCap for focused muscles), or trim non-emphasized muscles first / exclude the emphasis bonus sets from the trimmable pool, so emphasis survives the cap.

## 13. Cardio & settings (4)

> Week-window semantics and a couple of dead/inconsistent settings controls.

- [x] **🟡 MED** Hero "minutes this week" uses a rolling 7-day window but is labelled and visualized as the ISO Mon–Sun week
  - [ui/cardio/CardioViewModel.kt:45-49, 62](forge-android/app/src/main/java/com/forge/app/ui/cardio/CardioViewModel.kt#L45)
  - Fix: Derive the week-start used for observeMinutesSince from the ISO week (isoWeekStart.atStartOfDay(zone).toEpochMilli()), matching what CardioScreen and buildDailyMinutes use, rather than…
- [x] **🟡 MED** Switching cardio from dedicated days to a weekly minute goal leaves stale cardio days in the program
  - [ui/settings/SettingsViewModel.kt:167-177](forge-android/app/src/main/java/com/forge/app/ui/settings/SettingsViewModel.kt#L167)
  - Fix: Make setCardioWeeklyTargetMin symmetric: when min>0 and cardioDaysPerWeek was non-zero, call regenerateProgram() (and surface the same status message) so the cardio days actually disappear from the…
- [ ] **⚪ LOW** Holiday/Vacation settings row is a dead no-op that silently does nothing — _DEFERRED: the vacation feature is entirely unbuilt (VacationDao exists but no repository/UI/logic uses it). Product decision: build the feature or remove the placeholder row_
  - [ui/settings/SettingsMainList.kt:41-44](forge-android/app/src/main/java/com/forge/app/ui/settings/SettingsMainList.kt#L41)
  - Fix: Either remove the row until the feature exists, or wire it to a real page/dialog. If kept as a placeholder, visually disable it (greyed, no chevron) so it doesn't read as tappable.
- [x] **⚪ LOW** FormatPage timezone list shows no selection when device timezone is not one of the 12 presets
  - [ui/settings/SettingsSubPages.kt:101-121](forge-android/app/src/main/java/com/forge/app/ui/settings/SettingsSubPages.kt#L101)
  - Fix: Either add the device default to the option list when it isn't already present, or render a synthesized 'current timezone' row at top when state.timezone is not in TIMEZONE_OPTIONS so the active…

## 14. Session flow & misc UI-state (11)

> Smaller correctness/validation/state cleanups across the session and misc screens.

- [x] **🟡 MED** Reps-field 'Done' action logs a set with no validation on the weight, bypassing the disabled-button guard
  - [ui/gym/train/components/SetInputRow.kt:115-119, 190](forge-android/app/src/main/java/com/forge/app/ui/gym/train/components/SetInputRow.kt#L115)
  - Fix: Have submitSet() also check that weight is valid (mirror canSubmit: weight.isNotBlank() and parses), and no-op otherwise; or wire onDone to only fire when canSubmit is true.
- [x] **⚪ LOW** searchNotes does not escape LIKE wildcards in the user query
  - [data/db/dao/LoggedExerciseDao.kt:145-153](forge-android/app/src/main/java/com/forge/app/data/db/dao/LoggedExerciseDao.kt#L145)
  - Fix: Escape LIKE metacharacters before binding (e.g. replace \\, %, _ with an escaped form) and add an ESCAPE clause: ... LIKE '%' || :query || '%' ESCAPE '\\', passing a pre-escaped query; or switch to…
- [x] **⚪ LOW** setRestTimerOverride UPDATE is a silent no-op when no customization row exists yet (mislabeled 'Upsert') — _removed the dead DAO method; the repository already does a correct get-then-upsert_
  - [data/db/dao/ExerciseCustomizationDao.kt:21-23](forge-android/app/src/main/java/com/forge/app/data/db/dao/ExerciseCustomizationDao.kt#L21)
  - Fix: Either remove this method (the repository already handles upsert correctly), or implement true upsert semantics (INSERT ... ON CONFLICT, or a @Transaction get-or-create + UPDATE), and fix the comment…
- [x] **⚪ LOW** isNewSession relies on setCount, which is only stamped at finish — stale (always 0) during an active session
  - [data/repo/WorkoutRepository.kt:102-105](forge-android/app/src/main/java/com/forge/app/data/repo/WorkoutRepository.kt#L102)
  - Fix: Derive emptiness from actual logged data rather than the denormalized setCount, e.g. `s.finishedAt == null && loggedExerciseDao.forSession(sessionId).isEmpty()` (or a COUNT(*) of logged_set rows for…
- [x] **⚪ LOW** WeightParser accepts negative numbers, producing negative volume and false PRs
  - [domain/parser/WeightParser.kt:45](forge-android/app/src/main/java/com/forge/app/domain/parser/WeightParser.kt#L45)
  - Fix: Reject non-positive results in the bare-number and lb branches, e.g. `text.toDoubleOrNull()?.takeIf { it >= 0 }`.
- [x] **⚪ LOW** Privacy mode (FLAG_SECURE) applied asynchronously despite comment claiming synchronous application — first frame can be unsecured
  - [MainActivity.kt:77-80](forge-android/app/src/main/java/com/forge/app/MainActivity.kt#L77)
  - Fix: Apply FLAG_SECURE before setContent using a synchronous first read of the preference (e.g. runBlocking { settingsRepo.privacyMode.first() }) as the comment describes, then keep the collector for live…
- [x] **⚪ LOW** Journal text can be lost: DismissSummary navigates back (clears VM) before the separately-launched UpdateJournal write completes
  - [ui/gym/train/DaySessionHandlers.kt:144-154](forge-android/app/src/main/java/com/forge/app/ui/gym/train/DaySessionHandlers.kt#L144)
  - Fix: Persist the journal inside the DismissSummary coroutine before sending PopBack (pass journal into DismissSummary), so the write is sequenced ahead of navigation rather than racing a second coroutine…
- [ ] **⚪ LOW** isNewSession() result is computed then discarded; pre-session picker (showPreSessionPicker) is never shown — _DEFERRED: the pre-session picker UI is unbuilt (showPreSessionPicker has no renderer — only a state flag + dismiss handler). Build-or-remove product decision. (isNewSession's own logic bug is now fixed.)_
  - [ui/gym/train/DayViewModel.kt:91](forge-android/app/src/main/java/com/forge/app/ui/gym/train/DayViewModel.kt#L91)
  - Fix: Use the result: `_state.update { it.copy(showPreSessionPicker = workoutRepo.isNewSession(sessionId)) }` (or remove the call if the picker is intentionally retired).
- [x] **⚪ LOW** NoteField pin/unpin toggle compares trimmed text for the label but untrimmed text for the action
  - [ui/gym/train/components/NoteField.kt:88-89](forge-android/app/src/main/java/com/forge/app/ui/gym/train/components/NoteField.kt#L88)
  - Fix: Use a single, trimmed comparison and store the trimmed value consistently: `val t = text.trim(); onPinNote(if (currentPinnedNote == t) "" else t)` and label off the same `t`.
- [x] **⚪ LOW** Notes search treats user input as a LIKE pattern — '%' and '_' act as wildcards
  - [data/db/dao/LoggedExerciseDao.kt:149](forge-android/app/src/main/java/com/forge/app/data/db/dao/LoggedExerciseDao.kt#L149)
  - Fix: Escape LIKE metacharacters in the query before binding, and add an ESCAPE clause: replace `%`,`_`,`\` with escaped forms in Kotlin and use `... LIKE :pattern ESCAPE '\'`. Alternatively use FTS or an…
- [x] **⚪ LOW** Trophy 'Up next' nudge is computed from unfiltered sections, ignoring the active filter
  - [ui/trophies/TrophiesScreen.kt:90-92](forge-android/app/src/main/java/com/forge/app/ui/trophies/TrophiesScreen.kt#L90)
  - Fix: Either always source `nextLocked` from the full catalog by design (and document it as a global hint), or derive it from `state.filteredSections` so it matches what the user is currently viewing; hide…

